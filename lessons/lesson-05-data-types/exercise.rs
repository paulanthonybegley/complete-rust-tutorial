// Lesson 5: Data Types — Exercise
// Complete the TODOs. This file compiles once all TODOs are filled.
// Run:
//   rustc exercise.rs && ./exercise

use std::mem::size_of;

fn main() {
    // TODO 1: Declare a variable `age` of type u8 with value 30.
    // println!("Age: {}", age);

    // TODO 2: Declare a variable `balance` of type f64 with value 1234.56.
    // println!("Balance: {}", balance);

    // TODO 3: Create a tuple (i32, bool, char) and destructure it into three variables.
    // let my_tuple = (7, true, 'x');
    // let (n, flag, letter) = ...;
    // println!("{} {} {}", n, flag, letter);

    // TODO 4: Create an array of 4 i32 values and print the 3rd element (index 2).
    // let my_array = [10, 20, 30, 40];
    // println!("3rd element: {}", my_array[...]);

    // TODO 5: Print the byte size of a u64.
    // println!("u64 size: {} bytes", size_of::<...>());
}
