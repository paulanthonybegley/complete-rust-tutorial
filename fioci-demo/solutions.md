# fioci-demo — solutions

Ex.1: `docker compose up -d`; `/api/s3` now shows `"mode":"live-compose"`.
Ex.2: add the concept + template; `CONCEPTS.size()` grows the meter.
Ex.3: `minio` console at :9001 (fioci/fioci12345), create bucket, PUT through
     the S3 endpoint. Ex.4: add e.g. `memcached:11211` to compose + concept.
