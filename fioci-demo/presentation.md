# fioci-demo — 5-slide pitch

1. "Everybody's cloud, no account" — the five primitives.
2. Each primitive = one docker-compose service (1:1 mapping table).
3. The API never lies: `mode: simulated` vs `live-compose`.
4. htmx refreshes JSON into the station page without a page reload.
5. Next: swap any container for its AWS bring-your-own-host twin.
