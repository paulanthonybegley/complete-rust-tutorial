# fioci-demo — 45-minute lab plan

- 0:00  Tour the station grid, open `/api`.
- 0:10  `docker compose up -d`, watch every probe turn live.
- 0:20  Read `FiociService` — the port probe + mode switch.
- 0:30  Swap S3 backing to a real upload through MinIO console (:9001).
- 0:40  Debrief: which "cloud" fears are really just naming?
