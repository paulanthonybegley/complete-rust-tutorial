# test-the-system.md — prove the system, not the page

1. **White-page rule.** A blank page is a template reading data that
   doesn't exist (EL1008E). If a page is white, the template references a
   field the data lacks — fix the data, never the browser.
